apiVersion: v1
kind: Service
metadata:
  name: photo-archive-api-service
spec:
  selector:
    app: photo-archive-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
  type: ClusterIP
